<!-- Dropdown Structure -->
<nav class="grey darken-3" style="z-index: 99999">
    <div class="nav-wrapper">
        <div class="container">
        <a href="{{ path('home') }}" class="brand-logo logo bounce"><img src="{{ asset('build/images/logo.png') }}" alt=""></a>
        <a href="#" data-target="mobile-demo" class="sidenav-trigger"><i class="material-icons">menu</i></a>
        <ul class="right hide-on-med-and-down">
            <li><a href="{{ path('home') }}">Accueil</a></li>
            <li><a class="dropdown-button" href="#" data-target="dropdown-blog"><i class="material-icons right">arrow_drop_down</i>Blog</a></li>
            <li><a class="dropdown-button" href="#" data-target="dropdown1"><i class="material-icons left">
                    person
                    </i>{{ app.user.username }}<i class="material-icons right">arrow_drop_down</i></a></li>
        {% if is_granted('ROLE_USER') %}
        <li class="nav-item">
            <a class="nav-link tooltipped" data-position="bottom" data-tooltip="Déconnexion" href="{{path('logout')}}"><i class="material-icons">
                power_settings_new
                </i></a>
        </li>
    {% else %}
        <li class="nav-item {% if mainNavLogin is defined %}active{% endif %}">
            <a class="nav-link" href="{{path('login')}}">Connexion</a>
        </li>
        <li class="nav-item {% if mainNavRegistration is defined %}active{% endif %}">
            <a class="nav-link" href="{{path('app_registration_register')}}">Inscription</a>
        </li>
    {% endif %}
        <!-- Dropdown Trigger -->
        </ul>
        <ul id="dropdown1" class="dropdown-content">
            <li><a href="{{ path('fos_user_profile_show') }}">Profil</a></li>
            {% if is_granted('ROLE_ADMIN') %}
            <li class="divider"></li>
            <li><a href="{{ path('admin') }}">Administration</a></li>
            {% endif %}
        </ul>
        <ul id="dropdown-mobile" class="dropdown-content">
                <li><a href="{{ path('fos_user_profile_show') }}">Profil</a></li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class="divider"></li>
                <li><a href="#!">three</a></li>
                {% endif %}
        </ul>
        <ul id="dropdown-blog" class="dropdown-content">
            <li><a href="{{ path('blog') }}">Consulter</a></li>
            <li class="divider"></li>
            {% if is_granted('ROLE_ADMIN') %}
            <li><a href="{{ path('admin_entries') }}">Gérer les posts</a></li>
            {% endif %}
        </ul>
    </div>
    <div id="bar"></div>
</div>

</nav>

<!-- Menu sidebar mobile -->
<ul class="sidenav" id="mobile-demo">
        <li><a href="{{ path('home') }}"></a></li>
        <li><a href="{{ path('home') }}"><i class="material-icons">
                home
                </i>Accueil</a></li>
        <li><a class="dropdown-button-mobile" href="#" data-target="dropdown-mobile"><i class="material-icons left">
                person
                </i>{{ app.user.username }}<i class="material-icons right">arrow_drop_down</i></a></li>
    {% if is_granted('ROLE_USER') %}
    <li class="nav-item">
        <a class="nav-link tooltipped" data-position="right" data-tooltip="Déconnexion" href="{{path('logout')}}"><i class="material-icons">
            power_settings_new
            </i></a>
    </li>
{% else %}
    <li class="nav-item {% if mainNavLogin is defined %}active{% endif %}">
        <a class="nav-link" href="{{path('login')}}">Connexion</a>
    </li>
    <li class="nav-item {% if mainNavRegistration is defined %}active{% endif %}">
        <a class="nav-link" href="{{path('app_registration_register')}}">Inscription</a>
    </li>
{% endif %}
    <!-- Dropdown Trigger -->
  </ul>
      